{
  "name": "jadehandforward",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html hunters.html api-config.js public/ && (cd backend && npm install) && echo 'Build complete. Public directory:' && ls -la public/",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.47.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "stripe": "^17.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
